<h1>Listing ships</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ships.each do |ship| %>
  <tr>
  	<td><%= ship.ship_type.name %></td>
    <td><%= link_to 'Show', [@game, ship] %></td>
    <td><%= link_to 'Edit', edit_game_ship_path(@game, ship) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ship', new_game_ship_path(@game) %>
